<!-- 轮播图 -->
<view class="mt-swiper">
    <swiper indicator-dots autoplay circular>
        <swiper-item wx:for="{{swiperList}}" wx:key="index">
            <navigator>
                <image src="{{item.img_src}}" />
            </navigator>
        </swiper-item>
    </swiper>
</view>
  
